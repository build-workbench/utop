# 2025-09-25 初始化项目

- 新建目录结构：`cmd/htop-win-go/`, `internal/ui/`, `internal/metrics/`, `changelog/`。
- 添加 `go.mod`。
- 实现系统指标采集：`internal/metrics/system.go`。
- 实现进程采集：`internal/metrics/processes.go`（基于 Times 差分计算 CPU%，使用 `runtime.NumCPU()`）。
- 实现 TUI：`internal/ui/ui.go`（tview 结构、快捷键、表格渲染）。
- 添加入口：`cmd/htop-win-go/main.go`。
- 添加 `README.md`。
