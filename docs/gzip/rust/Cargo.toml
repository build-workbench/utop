[package]
name = "rgzip"
version = "0.3.0"
edition.workspace = true
license.workspace = true
description = "A minimal gzip CLI tool in Rust (Linux-ready)."

[dependencies]
clap.workspace = true
flate2.workspace = true
